services:
  - type: web
    name: portfolio-backend
    runtime: node
    buildCommand: pnpm install && pnpm build && pnpm db:seed
    startCommand: node dist/src/main.js
    envVars:
      - key: PORT
        value: 10000
