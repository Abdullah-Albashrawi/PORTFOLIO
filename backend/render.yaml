services:
  - type: web
    name: portfolio-backend
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install && pnpm build && pnpm db:seed
    startCommand: npx pnpm start:prod
    envVars:
      - key: PORT
        value: 10000
